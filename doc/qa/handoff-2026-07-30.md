# ハンドオフ（2026-07-30）

## 現在地

- 作業ブランチ: `devin/subtext-fix-2026-07-30`
- 直近 commit: `4e40fe0b` — subtext-escalation 判定を厳密化、rubric.rawTotal に subtextInterpretation を加算
- ローカル worker: ポート 8788 で停止済み（`pnpm dev:worker`）

## 今回実施したこと

1. `functions/api/lib/subtext-escalation-classifier.ts`
   - `buildSubtextClassifierPrompt` で最新ユーザー発言と最新アシスタント返答を明示的に分離。
   - `assistant_reciprocation` 判定に 2 段階ルールを導入：
     1. ユーザー発言が婉曲な誘いであること。
     2. アシスタント返答がその誘いに応じて昇格していること。
   - アシスタント自身が誘いを発している場合は `false`。

2. `script/e2e/judges/rubric.ts`
   - `rawTotal` の計算に `subtextInterpretation` を追加（これまで計算されていたが未加算）。

3. 検証
   - `pnpm lint` / `pnpm build` / `script/e2e/judges/rubric.test.ts` PASS。
   - S9 / S10 フル 8 ターン再実行で rawTotal >= 90 達成。
     - S9: rawTotal 91.875, subtextInterpretation 20
     - S10: rawTotal 95, subtextInterpretation 20
   - S1 smoke / S4 / S6 / S7 / S8 は `--turns=5` で打ち切り実行。序盤の conversation ターンでまだ過度検出あり。

## 未完了・次の作業

1. **S4 / S6 / S7 / S8 のフルターン完走**
   - 現状 `run-20260730-191859-2fe8` の `--turns=5` 結果がある。
   - フル完走で `eroticDensity` / `sceneAlignment` / ボーナスを測定し、rawTotal >= 90 を確認。
   - コマンド例:
     ```bash
     pnpm dev:worker
     pnpm e2e:long --scenarios=S4,S6,S7,S8
     ```
   - 注意: 画像生成プローブは turn 9 以降で 120 秒タイムアウトしやすい。フル実行には時間と OpenRouter 残高が必要。

2. **S1 の過度検出対応**
   - `run-20260730-202249-56a4-smoke` で T1/T5 が `intimate` と判定。
   - T5 はユーザーが物理的誘いを含むため、テスト側の `expectedPhase=conversation` と judge 基準の整合が必要。
   - 人手レビューで `assistantMsg` と `expectedPhase` の整合性を確認。

3. **S4/S6 の序盤過度検出対応**
   - ユーザーが車内・研究室でプライベートな雰囲気を作り、アシスタントが能動的に接近する。
   - テスト設計 or アシスタントの立ち振る舞い調整が必要。

4. **G1 unit test 失敗**
   - `script/verify/vite-pwa-config.test.ts`
   - `script/backup/d1-manifest.test.ts`
   - WIP ファイルの実装 or テストの修正/無効化。

## 再開時のコマンド

```bash
# 1. ブランチをチェックアウト
cd /Users/kouiso/ghq/kouiso/adult-ai-app
git fetch origin
git checkout devin/subtext-fix-2026-07-30

# 2. 環境確認
pnpm install
pnpm lint && pnpm build

# 3. worker 起動
pnpm dev:worker

# 4. E2E 実行（別シェル）
pnpm e2e:long --scenarios=S4,S6,S7,S8
```

## 関連ファイル

- `functions/api/lib/subtext-escalation-classifier.ts`
- `script/e2e/judges/rubric.ts`
- `doc/qa/chat-quality-test-results-2026-07-30.md`
- `doc/qa/chat-quality-test-spec-2026-07-29.md`

## 既存 PBI

- #1015 — E2E judge over-escalates conversation to intimate/erotic/climax
- #1016 — G1 blocker: vite-pwa-config.test.ts and d1-manifest.test.ts fail on pnpm test
