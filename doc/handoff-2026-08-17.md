# ハンドオフ 2026-08-17 → 次の担当（Cloud Claude 等）

前任がコンテキスト上限に近づいたので引き継ぐ。**最初に読む順は 1 → 2 → 3。**

1. この文書（現状・地雷・順番）
2. `doc/backlog-2026-08-17.md` — **台帳。ここに無いものは対応しとらん**と読める状態を保っとる
3. `/root/.claude/plans/exitplan-aedjvenswvbus0vzaaaaagaaaaaaaa-eager-kernighan.md` —
   計画本体（L1/L2/L3 の完成条件、`/goal` の継続契約）。**8/16 時点なので古い主張が残っとる。**
   食い違ったら台帳が正

---

## 0. 鍵・認証（値はこの文書に無い）

**値は書いとらん。**`.env` は git-crypt 暗号化で、前任のセッションでは
`.prod-verify.vars` の読み取りが権限分類器にブロックされた。つまり前任は**平文を持っとらん**ので
書き写せんかった。局長がローテするつもりでも、コミットされる文書へ値を置くのは避ける。

**局長へのお願い:** 次のセッションのチャットへ直接貼ってください。ファイルには置かんでええです。

### チャット・検証に必要なもの

| 変数名 | 用途 | 無いとどうなる |
|---|---|---|
| `OPENROUTER_API_KEY` | チャット生成（`deepseek/deepseek-chat`）。**これが本体** | 生成が一切でけへん |
| `AUTH_TOKEN` | 自前 API の Bearer 認証 | ハーネスが 401 |
| `NOVITA_API_KEY` | 画像生成（モデルは `waiNSFWIllustrious_v90_1187991.safetensors` 固定） | 画像だけでけへん |
| `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` | `wrangler d1 execute --remote`、デプロイ | 本番 D1 が読めん |

### 任意（無くても進める）

`AUTH_SIGNING_KEY` / `BASIC_AUTH_USER` / `BASIC_AUTH_PASS` / `LOCAL_AUTH_BYPASS` /
`RUNWARE_API_KEY` / `ATLASCLOUD_API_KEY` / `CLAUDE_SESSION_TOKEN` /
`MONTHLY_COST_LIMIT_CENTS` / `DAILY_REQUEST_LIMIT` / `MAX_QUALITY_RETRIES` /
`GH_APP_ID` / `GH_APP_PRIVATE_KEY` / `GH_APP_INSTALLATION_ID` / `SLACK_ERROR_WEBHOOK_URL`

定義は `functions/api/lib/route-context.ts:152-181`（`Bindings` 型）。

### 入手経路

- **git-crypt:** `.env` は暗号化済み。鍵（`\0GITCRYPTKEY`, v2, 148 bytes）は局長が持っとる。
  `git-crypt` は**このコンテナに入っとらんかった**ので、入れるか AES-256-CTR で直接復号する
- **ハーネスが読む場所:** `.prod-verify.vars`（`.gitignore:32`。`script/verify/vlong-session-dogfood.ts:101-115`）。
  `BASE_URL` / `AUTH_TOKEN` を `KEY=value` で置く
- **本番 D1 の id:** `b617800c-cf4c-4122-adbb-ce872a258da6`（`adult-ai-db`）。**秘密やない**

### 課金について（局長の許可済み）

> 「aiテストはapi課金が走ってしまうがこのプロダクトの品質向上のテストのためには
> .envにあるものは許可なく走らせて構わない」

**都度確認は要らん。**参考値: 2026-08 の `monthly_cost_cents` は 21,245（7 月は月全体で 1,830）。
`COST_ESTIMATES.chat = 10` の内部単位で、1 チャット = 10。

---

## 1. いまのブランチと PR

| ブランチ | 中身 | 状態 |
|---|---|---|
| `fix/chat-ui-and-sheet-followups` | **本命。**PR #1462（draft）。UI 修正 13 件＋既定経路の交互ルール＋呼びかけ連投検出＋A5 の前戯ゲート | CI 待ち |
| `docs/dogfood-evidence-2026-08-17` | 読解アーム 17 本（298 ファイル）。**L2(a) の証拠**。PR 未作成 | 要 PR |
| `fix/phase-pacing` | A5 のぶん（#1462 へ cherry-pick 済み。参照用） | 取り込み済み |
| `fix/repetition-threshold` / `fix/relationship-stage-vocabulary` / `fix/chat-scroll-and-selection` | 同上（cherry-pick 済み） | 取り込み済み |
| `fix/prose-and-pacing` | **取り込んどらん。**全部 `isVeryLongResponse` ゲートの中で、出荷既定 medium へ届かん | 要書き直し |

**プレビュー:** https://fix-chat-ui-and-sheet-follow.adult-ai-chat.pages.dev
（マージ前に局長が触って判定できる。**Pages のプレビューは本番 D1 を共有する**ので migration の効果も出る）

**#1462 の CI で「Add PR Labels」が赤:** **原因は未解明。**前任の仮説は 2 つとも潰れた。

- 症状: `gh api --paginate "repos/$REPO/pulls/$PR_NUMBER/files"` が
  `403 Resource not accessible by integration`。ラベル計算より前の 1 本目のコマンドで落ちる
- **仮説1「一時的な権限のブレ」→ 却下。**同じブランチで 12:55/13:03/13:07 に 3 回成功して、
  13:41 以降は連続で失敗しとる
- **仮説2「差分が 2 万行を超えたから」→ 却下。**`docs/dogfood-evidence-2026-08-17`
  （299 ファイル・16,428 行）は**成功**しとって、`fix/chat-ui-and-sheet-followups`
  （159 ファイル・約 4,500 行）は**失敗**しとる。**大きい方が通っとる**ので行数やない
- 残る差: 成功したのは PR の `opened` イベント、失敗しとるのは `synchronize`。
  `pr-labeler` ジョブは `concurrency: cancel-in-progress: true` を持っとる
  （`.github/workflows/pr-auto-setting.yml:37-39`）。ただしログには 403 のレスポンスが
  はっきり出とって、キャンセルの体裁やない
- **ラベル付けだけのジョブで、コードには一切関係無い。**直すならワークフローの
  トークン権限を見る話。**コードを触って直そうとせんこと**

前任がここで一度「行数のせい」と断定して局長へ報告してもうた。**断定する前に、
反例（大きい方が通っとる）を探すこと。**

---

## 2. 棄却された診断（**ここを読まんと同じ穴を掘る**）

前任が自分で潰した分。**やらんでええ作業のリスト**として読む。

| 落ちた主張 | 正しかった事実 | やらんでええこと |
|---|---|---|
| **A1「履歴を毎ターン全部送るので窓を越えてシートが落ちる」** | **半分だけ死んだ。**⚠️**前任はここでも medium/very_long の罠を踏んだ**——「窓は越えん」の検算は `very_long`（deepseek, 163,840）に対してしかやっとらん。**出荷既定 medium の conversation/intimate/afterglow は `requestedModel` = qwen（32,768）へ行く**（`route-context.ts:1334`、`:1320` のコメント「会話は qwen」）。実測で 15 ターン 32,246 トークン（境界）・20 ターン 41,922（超過）。**既定設定では turn 15〜20 で窓を越える。**／死んどるのは「先頭が落ちる」の方: 圧縮設定は 0 件、OpenRouter の既定圧縮は 8,192 トークン以下のみ・削るのは中央。`trimChatMessagesToBudget`（`:707-743`, `CHAT_TRANSPORT_MAX_CHARS=180_000` `:6642`, `CHAT_HISTORY_MAX_TURNS=25` `:6646`, 適用 `[[route]].ts:1035, :1409`）で切っとるのも事実 | very_long 前提の「窓は越えん」を根拠に何もせんこと。**medium/qwen 側は別途測ること** |
| **C6「反復検出は語単位／閾値の問題」** | どっちも誤り。文単位の検出は既にある（`quality-guard.ts:344-361`）が、見とるのは**文全体の一致率**（bigram Jaccard 0.72）。呼びかけだけ共有して残りが違う 6 行は 0.2 前後 | **閾値いじり全部。**要ったのは測る場所の追加 |
| **A3「埋め込み・ベクタ検索が一つも無い」** | 言い過ぎ。テーブル・migration・ingest は在る（`src/schema/rag-chunk.ts`、`drizzle/0031`/`0032`、`script/rag/ingest.ts`） | RAG をゼロから作る見積り。**要るのは検索クエリだけ**（`functions/` に `ragChunk` を読む行が 0 件） |
| **A2「memory が壊れとる」** | 「一度も作っとらん」が正しい。読む側は動く（`route-context.ts:5525` → `memory-relevance.ts:72` → `prompt-builder.ts:582`）。書く側に生産者が 0。`<remember>` を要求するプロンプトが 1 本も無く、`/api/memory/extract` のクライアント呼び出しも 0 件。**`autoExtractMemories` は既定 true でトグルまで出とるが何も起きん死んだスイッチ** | 抽出器の作り直し。要るのは「出せ」の 1 行か、クライアントからの呼び出し |
| **「phase13 で invented history が直った」** | 1 アームだけの誤判定。phase14/15 のさくら t1 に戻っとった | 「1 アームで消えた」を直った証拠にすること |

---

## 3. 地雷（前任が実際に踏んだ順）

### ★ 3-1. `very_long` で直しても局長には届かん

**出荷既定は `medium`**（`src/store/settings-store.ts:26, 80`）。
`route-context.ts:4468-4470` の `isLongResponse` は「`very_long` **または** phase が erotic/climax」で真になるので、
medium でも長文指示は付く。**ただし付くのは `VERY_LONG_*` やのうて `EROTIC_LONGFORM_HINT` の側**
（`:4473-4482`）。

**これで 3 回無駄にした。**判定の仕方:

```
responseLength=medium で組んだ指示の中に、その修正が入っとるか。
無いなら未完了。
```

`fix/prose-and-pacing` が丸ごとこれで死んどる。

### ★ 3-2. 読解アームを `very_long` で測ってはいかん

22 アーム全部が `very_long` やった。ハーネスの既定を `medium` へ直して、
食い違ったら落ちるテストを入れた（`script/verify/vlong-session-request-shape.test.ts`）。
`--length very_long` は明示した時だけ。

### ★ 3-3. 指示を足して直そうとせん（Gate 0）

既に同じ指示がある時、足しても勝たん。A5 の実例:

- `prompt-variant-defaults.ts` の同じ [Phase progression guideline] ブロックの中で、
  「親密・性的キューがあれば **MUST escalate**」が「このフェーズを**最低2ターン維持**」の**2 行上**にあった
- 本番 D1 で位置まで確認済み。`conversation_xml_hint` は MUST が 741 / hold が 966、
  `scene_response_structure` は 806 / 1012。**MUST が先に読まれて上書きしとった**
- 直し方は**勝っとる側を削る**（migration `0069`）。負けとる側を強化せん
- 段の一覧も削除した。`SCENE_CONTEXT_MESSAGES.erotic`（`route-context.ts:1927`）が
  `Ignore any earlier prompt that describes erotic as 'clothes off / fingertip stimulation...'` で
  **末尾から否定しとった**ので、打ち消される側やった

### 3-4. 隔離環境の成果物はリポジトリに入っとらん

scratchpad の複製リポジトリで走らせた 17 アームがリポジトリ側に無く、消える寸前やった。
**コンテナは使い捨て。**`.work/e2e-results/` は `.gitignore:55` で再包含されとるので commit できる。

### 3-5. migration の REPLACE は当たったか確かめる

0067 の 1 本目は何も一致せんかったのに「2 rows changed」と出た。
実際の文字列は 「嫌う：暴力、無理やり、貶める言葉」で、狙っとったのは「相手を貶める言葉」やった。
**当ててから `instr()` で確認する。**

### 3-6. エージェントの自己申告を証拠にせん

前任が 3 回、エージェントの主張を自分で開いて確かめて、うち 2 回は**エージェントの方が正しかった**
（前任の仮説が間違っとった）。1 回は前任が追加検証して、`platform_scene` が 2 本目の UPDATE に
当たらんことを確認して初めて安全と判断した。**両方向に確かめる。**

### 3-7. 会話履歴を消すと局長の開いとる画面が壊れる

前任が本番の履歴を全削除して、局長のライブのチャットが送信でけへんくなった。
**キャラは消さん**（局長の明示指示）。履歴を消す時は局長にリロードを頼む。

### 3-8. 同じ push を並行で何本も投げるとコンテナが詰まる

pre-push フックが `ci:fast`（lint + 全テスト）を回す。4 コアに対して load 22 まで行った。
**push は 1 本ずつ。**バックグラウンドに投げて待つ。

---

## 4. 進める順（局長の痛み ÷ リスク）

**1. #1462 を出す** — 費用ゼロ
原因確定＋テスト同梱のものが 20 コミット溜まっとる。局長が判定できる完成品を出さんまま
新規調査を積むのが一番の無駄。`task ci` を通してから ready にする。

**2. `docs/dogfood-evidence-2026-08-17` の PR を作る** — 費用ゼロ
L2(a) の証拠。draft で出す。

**3. migration `0069` を本番 D1 へ当てる** — 費用ゼロ、**未実施**
前任は**権限分類器にブロックされて当てられんかった**（MCP の d1 write と
`wrangler d1 execute --remote --file` の両方）。**局長の許可か、権限ルールの追加が要る。**
当てんとコード側の `hasSustainedForeplay` だけが入って「片方だけ触った状態」になる。
確認クエリ（読み取りだけなら通る）:
```sql
SELECT id, instr(body,'MUST escalate') AS at_must,
       instr(body,'このフェーズを最低2ターン維持すること') AS at_hold
FROM prompt_variant;
```
`at_must` が 0 になっとれば適用済み。

**4. PBI-F の続き（出荷既定の長さがまだフロアに届かん）** — 有料 20 コール
2026-08-18 に末尾差し込みを全長さへ広げて 172 字 -> 571/776 字まで来たが、
erotic のフロア 960 にはまだ届かん。**一番怪しいのは `promptMinChars` の overshoot が
very_long にしか無いこと**（`[[route]].ts:1300-1305`）。フロアちょうどを狙わせると下振れる。

**5. PBI-G（霜月鈴が出荷既定で erotic へ一度も上がらん）** — 3 の後
phase17 で t7〜t10 が 4 ターンとも intimate のまま。**3（0069）を当ててから測る。**

**6. 出荷既定(medium)の残りの矛盾を削る** — 費用ゼロ
`EROTIC_LONGFORM_HINT` の順序指示と、closing rule の `の順で出力` は削除済み（`663e47e3`）。
`CLIMAX_LONGFORM_HINT` に `★very_long指定時のclimax展開` という**その経路では到達不能な節**が
残っとる。他にも medium 側だけ古い節がないか洗う。

**7. 品質ゲートの順番と本数を引き直す** — 費用ゼロ（検証は有料）
`getMaxQualityRetries` が全フェーズ 1 固定（`quality-guard.ts:1348-1352`）で、
チェックは**先に落ちた 1 個で打ち切り**（`:1370-1402`）。新しい検出器は列の後ろなので、
前のチェックが 1 回の再試行を食い切る。**新しい検出器を足す前にここ。**
実測 2026-08-18 phase18 のワーカーログ: `attempt 0 failed: wrong-first-person` -> `retry-dispatch {"category":"pov_wrong"}` で 1 回きりの再試行が POV に食われ、**長さの不足はそのまま配られとる**（`turn generation budget exhausted; returning repaired best attempt`）。PBI-F が長さで詰まっとる直接の理由がこれの可能性がある。

**8. A4（場面の状態）** — 設計変更
`buildSceneStateContinuity`（`route-context.ts:6262-6293`）は直近 14 通・**user 発言だけ**・
location/clothing/arousal の 3 スロットだけ。`:6265-6266` に「assistant の幻覚を拾うと
場所飛び・服装巻き戻しを助長する」と**意図的な除外**の理由が書いてある。
単純に assistant を読ませると幻覚を自己強化する側へ倒れるので、
「シート＞ユーザー発言＞キャラ宣言」の優先順位を持った状態表が要る。
**局長が毎ターン手で補っとる痛みに直で当たる唯一の項目。**

**9. A2（記憶の書き手を 1 人立てる）** → **10. A3（検索クエリだけ）**

---

## 5. PBI（コピペ用）

そのまま issue 本文に貼れる形。既存 issue は #1454〜#1461。

---

### PBI-A: migration 0069 を本番 D1 へ当てる

```
## 背景
A5（「まだ胸の段やのに手マンに入る」「1チャットで勝手に話の展開が進む」）は 2 層ある。
コード側（hasSustainedForeplay ゲート）は PR #1462 に入っとるが、D1 の champion プロンプト側が
古いままやと「片方だけ触った状態」になって効かん。

## 事実（本番 D1 で確認済み 2026-08-17）
同じ [Phase progression guideline] ブロックの中で、
「親密・性的キューがあれば MUST escalate」が「このフェーズを最低2ターン維持」の 2 行上にある。

| id | MUST の位置 | hold の位置 |
|---|---|---|
| prompt-variant-seed-conversation_xml_hint | 741 | 966 |
| prompt-variant-seed-scene_response_structure | 806 | 1012 |
| prompt-variant-candidate-...-climax-escalation-v1 | 811 | 1017 |

MUST が先に読まれて hold を上書きする。指示を足しても勝たん。

## やること
drizzle/0069_phase_ladder_pacing_in_prompt_variant.sql を本番へ当てる。

## 完了条件
SELECT id, instr(body,'MUST escalate') FROM prompt_variant; が全行 0

## 注意
- 2 本目の UPDATE は段の一覧を消すが、prompt-variant-seed-platform_scene は
  その一覧を持っとらん（instr = 0）ので「最低2ターン維持」はそこに残る。確認済み。
- 前任は権限分類器にブロックされて当てられんかった。許可が要る。
```

---

### PBI-B: 出荷既定(medium)の経路に残っとる到達不能な節を洗う

```
## 背景
route-context.ts:4473-4482 は responseLength で別の長文指示を渡す。出荷既定は medium
(src/store/settings-store.ts:26,80) なので、VERY_LONG_* 側だけ直しても局長には届かん。
この見落としで 3 回無駄にした（交互ルール、fix/prose-and-pacing、圏点/段落リズム）。

## やること
1. CLIMAX_LONGFORM_HINT の「★very_long指定時のclimax展開」を削る（その経路では到達不能）
2. medium 側の EROTIC_LONGFORM_HINT / CLIMAX_LONGFORM_HINT を通しで読んで、
   very_long 前提の節・矛盾する節を洗い出す
3. very_long 側にしか無い品質指示を棚卸しして、medium へ要るものを移す

## 完了条件
responseLength=medium で組んだ指示を丸ごと吐いて目視。
到達不能な条件節がゼロ。矛盾する順序指示がゼロ。

## やってはいかんこと
- 指示を足して直す（Gate 0）。同じ指示が既にあるなら、位置か矛盾か閾値の問題
- 合意・同意・セーフワード・境界・配慮・倫理の枠を足す（prompt/instructions/no-injected-ai-filter.md）
```

---

### PBI-C: ~~霜月鈴が erotic に入った瞬間だけ地の文の壁になる~~ 【2026-08-18 解決・要実測】

```
## 前提が間違っとった
「霜月鈴だけ」やない。数え直すと壁になっとるのはキャラやのうて **フェーズ**。
  phase15 霜月鈴 t7  (erotic)     -> A17 D12 I1
  phase15 霜月鈴 t10 (afterglow)  -> A5 D5 A3 D5 A3 D5
  phase16 さくら   t5  (intimate)  -> A17 D17 I1
交互の指示を入れたのが erotic/climax の 4 本だけで、intimate と afterglow には
一度も入れとらんかった。

## 原因は「足りん」やのうて「矛盾しとる」（2 つ）
1. `[[route]].ts` の very_long 用 closing rule が、「交互に置け」と同じ文の中で
   「<action>、<dialogue>、<inner> **の順で出力し**」と名指ししとった。
   抽象的な「交互」より具体的な順序の方が勝つので、出るのは A の壁 -> D の壁 -> I。
2. 非 very_long ＝ **出荷既定** 側の closing rule は、外枠を
   `<response><action>...</action><dialogue>...</dialogue><inner>...</inner></response>`
   と 1 つずつ並べた完成形で書いとった。閉じ方の注意のつもりが構造の見本として届く。

## やったこと（`663e47e3`）
両方を消し、intimate と afterglow の長文指示へ交互の一文を入れた。
テスト 3 本（`interleave-narration-and-speech.test.ts`）を赤で確認してから直しとる。

## 残っとること
出荷既定で 20 ターン測った phase18 は、壁ゼロやが erotic のフロア 960 字に対して
571〜776 字で止まる。**壁は消えたが長さはまだ届いとらん。** PBI-F を先に。
```

---

### PBI-F: 出荷既定で応答が短すぎる（フロア 960 に対して 172 字）

```
## 事実（2026-08-18 phase17、medium・20 ターン）
  会話    88〜116 字
  intimate 168〜229 字
  erotic  172 / 176 字   <- フロアは 960 字
  climax  200 字
原因はフロアの値やのうて **置き場所**。#1344 が「長さ指示をユーザー発言の末尾へ置くと
システム指示より優先される」と実測して入れた差し込みが `isVeryLongResponse` で
閉じられとった。**出荷既定は medium なので、長さを実際に保っとる唯一の機構が
局長のターンへ一度も掛かっとらんかった。** 読解アーム 22 本が全部 very_long
やったので、誰も気づかんかった。

## やったこと
- `a664c41a` 末尾差し込みを全長さへ広げた。very_long は既存のブロックのまま、
  他の長さにはフロア・上限・交互ルールだけ持った短い版を出す
- `3d6bd73c` 目標をフロアの 1.3 倍に。very_long だけが +400 の overshoot を持っとって、
  他の段は目標＝フロアやった。ちょうどを狙わせると必ず下振れる
- `b569d1a2` 外枠の見本を 2 サイクル形へ／`<inner>` の畳み込みを全長さへ

## 実測（3 アーム、全部 medium・同じ台本）
             phase17(前)   phase19(中)   phase20(後)
  会話        88〜116       239〜303      213〜432
  intimate    168〜229      289〜511      493〜603
  erotic      172 / 176     857 / 586     445 / 442
  climax      200           771           581
  余韻        229           526           617
  壁          0/20(*)       2/20          0/16
  <inner>最大  1 ブロック    7 ブロック     1 ブロック
  <inner>字数  75           346           98
  (*) phase17 は短すぎて壁の形になれんかっただけ

phase20 は 16 ターン全部が `A1 D1 A1 D1 … I1`。壁ゼロ、inner は 1 ブロック 98 字以内。
phase19 で出た退行（inner 7 ブロック / XML 崩れ）は両方消えとる。

## 残っとること
**erotic はまだフロア 960 に届かん。しかもばらつきが大きい（445〜857）。**次に見るのは
1. `getMaxQualityRetries` が全フェーズ 1 固定（`quality-guard.ts:1348-1352`）。
   ワーカーログで `attempt 0 failed: wrong-first-person` -> `retry-dispatch {"category":"pov_wrong"}`
   を確認しとる。**1 回きりの再試行が POV に食われて、長さ不足はそのまま配られとる**
   （`turn generation budget exhausted; returning repaired best attempt`）。PBI-D と同じ根。
2. medium の erotic はレイテンシも危ない。phase20 の霜月鈴 t7 は 120 秒で
   クライアント側タイムアウトに当たって通しが止まった。

## やってはいかんこと
交互や長さの指示をもう一文足す。既に入っとる（届いとるかを先に確かめる）。
```

---

### PBI-G: 霜月鈴が出荷既定で erotic へ一度も上がらん

```
## 事実（2026-08-18 phase17、medium）
  霜月鈴 t7  intent=erotic    -> served=intimate
  霜月鈴 t8  intent=erotic    -> served=intimate
  霜月鈴 t9  intent=climax    -> served=intimate
  霜月鈴 t10 intent=afterglow -> served=intimate
さくらは同じ台本・同じ run で t7 から erotic へ入っとる。
ユーザー発言は「逃がす気ないんでしょ。分かってるよ。」「そのまま、上から」
「……出る。全部きみの中に。」。3 つ目でも上がらん。

## 関係しとるもの
- A5 のフェーズ機構（`classifySubtextEscalation` / `applyPhaseFloor`）
- migration 0069 が **本番にも local D1 にも当たっとらん**（PBI-A）
- `SUSTAINED_FOREPLAY_TURNS = 2` と `countForeplayTurns`（`src/lib/scene-phase.ts`）

## やること
1. PBI-A（0069）を先に当てる。当てんまま測っても切り分けにならん
2. local D1 にも当てて phase17 の台本を霜月鈴だけ 10 コールで測り直す
3. それでも上がらんなら `classifySubtextEscalation` の分類結果をログへ落として、
   どのターンで false が返っとるかを見る

## 注意
「erotic へ上げる」ためにユーザー発言のキーワードを増やさん。
台本は実ユーザーの打ち方に寄せてある。台本を変えると測っとるものが変わる。
```

---

### PBI-D: 品質ゲートの順番と再試行本数

```
## 事実
- getMaxQualityRetries が全フェーズ 1 固定（src/lib/quality-guard.ts:1348-1352）
- runQualityChecks は配列の先に落ちた 1 個で打ち切り（:1370-1402）
- 新しい検出器（呼びかけ連投・壁）は列の後ろに置いてある

## 問題
前のチェックが 1 回の再試行を食い切ると、後ろの検出器は事実上死ぬ。
検出器を足すほど、足した分が効かんくなる構造。

## やること
落ちたチェックの重さで再試行本数を決めるか、列の順番を「局長の痛みが大きい順」へ引き直す。
再試行はレイテンシと課金に直結するので、増やす前に実測を出す。

## 完了条件
回収した 370 ターン（.work/e2e-results/vlong-dogfood/）へオフラインで掛けて、
各検出器が何回発火するかの表。費用ゼロ。
```

---

### PBI-E: A4 場面の状態を事実として積む

```
## 事実
buildSceneStateContinuity（functions/api/lib/route-context.ts:6262-6293）は
- 直近 14 通のみ
- user 発言だけ（:6267 recentUserOnly）
- location / clothing / arousal の 3 スロットを正規表現で 1 つずつ
- phase === "conversation" なら即 return ""

:6265-6266 のコメント:「assistant の幻覚（ベッド/シーツ等）を拾うと勝手に場所飛び・
服装巻き戻しを助長する」= 意図的な除外。この理由は正しい。

## 問題
モデルが一度でっち上げた設定（学ラン・高校生）は次のターンで否定される場所が無い。
何をどこまでしたか・主導権・ゴムの有無・積み上がりも持っとらん。
局長が毎ターン手で補っとる（「結構ユーザー側でこうやってフォローしないと心もとない」）。

## やること
「シート ＞ ユーザー発言 ＞ キャラの宣言」の優先順位を持った状態表。
シートと矛盾する宣言は落とす。既存の scene_state テーブルに乗るか先に確かめる。

## やってはいかんこと
- assistant をそのまま読ませる（幻覚を永久化する）
- キャラの態度・気持ちを状態に持つ。それはシートの領域（no-injected-ai-filter.md）
```

---

## 6. 局長の常設指示（守る）

- **キャラ設定は局長のもの。**勝手に性格を変えん。**キャラを消さん**
- **合意・同意・セーフワード・境界・配慮・倫理の枠をプロンプトへ足さん。**特定の台詞を禁止語にせん。
  品質判定で受動的な反応そのものを不合格にせん（`prompt/instructions/no-injected-ai-filter.md`。**恒久**）
- **演者の層と出来事の層を分ける。**局長の言葉:
  「無理やりされるのがすごい嫌がるキャラとして登録されたとする。嫌がるのはキャラの振る舞い。
  でもチャット上は、嫌がっていても無理やりやってるという実際の行動を表すべき」
- **生々しさに上限をつけん。**ただし場面と相手の文体で判断する（上品な雰囲気が合う場面もある）
- **キャラは名前で呼ぶ。**「きみ」「あなた」で指さん
- **UI を優先せず、ユーザー体験を優先する**
- **調べれば分かる質問は調べる。**聞いてよいのは調べても分からん時だけ
- **LLM 採点をゲートにせん。**`/api/nukeru` を復活させん。点数へ最適化せん
- **`--no-verify` / `git reset` / main への force push は禁止**（`AGENTS.md`）
- `any` / `@ts-ignore` / `eslint-disable` 禁止
- コードコメントは日本語で「なぜ」だけ。コミットメッセージは英語 Conventional Commits。ドキュメントは日本語
- **画像は Novita 直・モデルは `waiNSFWIllustrious_v90_1187991.safetensors` 固定。**
  生成前に枚数と概算コストを局長へ出す

## 6.5. 「別人になる」の実測（2026-08-17 の並行 workflow。**次の担当の最優先候補**）

前任が verify したぶんだけ書く。ハーネスは
`/tmp/.../scratchpad/measure-context.ts`（コンテナが消えたら無い。作り直せる）。

**① 希釈が 23:1。**さくらのシート本文は 1,252 字。その周りにサーバが毎ターン
約 29,300 字の汎用指示を巻く。`msg[0]` は 18,992 字で、**シートはその末尾 6.6%**。
内訳: platform prefix 5,150 ＋ `CHAT_BASE_RULES` 12,579 ＋ シート 1,252。
`CHAT_BASE_RULES` の内訳は NAME IDENTITY 1,853 / SEX-POSITION 1,674 /
SAYLO-STYLE 1,576 / USER ADDRESS 1,227 / REALISM 1,058 / …。
**シートどおりに振る舞わせたいなら、ここを削るのが一番効く。**

**② 自己条件付け。**20 ターン地点のリクエストは 65,598 字で、**うち 52k はモデル自身の
過去出力**。シートは深さ 22% の位置で、その後ろに 63k 字が続く。真似する材料として、
シートより自分の汎用散文の方が圧倒的に多い。**これが「長くやるほど悪化する」の本体。**

**③ サーバがシートを主張しとらん。**`prependServerAuthoritativeCharacterSystemPrompt`
（`route-context.ts:1449`）と `stripUntrustedClientSystemMessages`（`:1446`）は
**`functions/api/__tests__/prompt-injection-boundary.test.ts` からしか参照されとらん。**
`/chat` ハンドラはどちらも呼ばん。人格の本文は **100% クライアント任せ**
（`ou-app.tsx:1468` → `activeCharacter?.systemPrompt ?? DEFAULT_SYSTEM_PROMPT`）。
**`activeCharacter` が null の時、クライアントは汎用の小説家プロンプトを送り、
かつ `characterId` も落とす**（`ou-app.tsx:1404` の `charId = activeCharacter?.id`）。
サーバは D1 から場面だけロックするので、**汎用の 20 歳が「どの女の子でもない誰か」になる。
これが 学ラン/高校生 の署名そのもの。**

**④ 記憶の要約が無いまま turn 25 で忘れる。**30 ターンで user #1〜#5、
40 ターンで #1〜#15 が消える。要約する物は何も無い（`<remember>` を要求する
プロンプトが 0 件、`/api/memory/extract` の呼び出しも 0 件）。

## 7. 残っとる未確定

- **局長の「学ラン」のターンが very_long の erotic やったか**は、もう分からん。
  前任が本番の会話履歴を全削除したので材料が無い
- **G1「画像が場面と一致しとらん」**は未診断。`buildImagePromptFromHistory` の中身と、
  img2img の参照元（avatar かサブ画像か）のどちらが支配的かを見る必要がある
- **#1458**（ト書きとシートが矛盾した時の優先順位）は**局長が自分で試したい**と言うたので起票のみ
- **F2**（画像 #910 item 3）は設計判断待ち
