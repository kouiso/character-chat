# 積み残し台帳 — 2026-08-20

## 1. 結論

70項目を全部いずれか1つへ落とした。**直した53件・反証で覆った5件・まだ残っとる9件・局長の判断待ち2件・不明1件**。前の台帳（08-17）から一番変わったんは「長さが主犯」やった診断が死んだことで、今日時点で一番重い単一欠陥は**反復（phase66で20ターン中16）**。分類作業中に前提の誤りを3つ見つけたんで、それも下に書いた（`quality-guard.ts` の場所、cross-turn の走査範囲、F2/G2 を判断待ちにしとったこと）。

---

## 2. まだ残っとるもの

重い順。次の一手は全部コード側の手で、プロンプトへ一文足す手は使わん。

| # | 何が | 重さ | 証拠 | 次の一手 |
|---|---|---|---|---|
| 1 | **反復**。同一ターン内の内容語の連呼、同じ身体反応の二度出し、前ターンの `<inner>`／段落の逐語再放送。既存の検出器が3形とも素通りさせとる | 大 | `doc/dogfood/vlong-2026-08-20-phase66.md` 冒頭「19/20 FAIL…最も重い単一原因は反復16/20」。phase54は13/20やったんで悪化しとる。実装側: `src/lib/quality-guard.ts:423` `checkWithinTurnRepetition` は文単位Jaccard 0.72と12〜22字の部分文字列一致だけ、`:676` `findCrossTurnRepetitionMatch` は8字以上の句が2つ以上。**単語頻度型の検出器はファイル内に一つも無い**（`grep 内容語/wordFrequency` = 0件） | (1) 同一ターン内の内容語頻度チェックを足す。除外は `first_person`／`address`／キャラ名／直前ユーザー発言に出た語／`verbal_tics`（これ入れんとシートどおりのターンが全部落ちる）。(2) `<inner>` ブロック単体を前ターンの `<inner>` と正規化して類似度で突き合わせる。(3) `dedupeMergedBlocks` の重複判定を直前ターンの本文まで広げる |
| 2 | **段のラチェット**。`applyPhaseFloor` が今も単調増加のまま（afterglow だけ特例）。本文に脱衣も挿入も出とらんのに climax が配られ、その先で着衣描写へ巻き戻る | 大 | `functions/api/lib/route-context.ts:9084-9092` を開いて現行構造を確認済み。`git log -S applyPhaseFloor` の直近の構造変更は 81f468d（台帳作成前）で以降ゼロ。症状: phase66 の Sakura-08→09 と Downer-08→09 が同型、Downer-10 で矛盾が伝播。migration 0069 は「MUST escalate」の一行を消しただけで、判定ロジックには触っとらん | フェーズ判定に「直前ターンまでに脱衣／挿入に当たる描写が出たか」のフラグを持たせ、立っとらんうちは climax を配らず erotic をもう1ターン続ける。判定は生成後の本文への検出でええ |
| 3 | **地の文が単語の途中で切れる**。タグは正しく閉じとるんで、開閉だけ見とる検出には引っかからん（新規・phase66 で発覚） | 中 | phase66 §6-3: Sakura-07「ふと自分の指が、あ…」Sakura-08「二人の影がくっ…」Downer-10「腰を深く沈めた瞬間、きみ…」の3本。`grep 破損/truncat/途中切れ src/lib/quality-guard.ts` に該当検出器なし | `<action>` の末尾が句点・？・！以外で終わり、かつ名詞・助詞で切れとる場合を破損としてリトライへ回す。**`<dialogue>` は対象外**（霜月鈴のシートが語尾の途切れを指定しとる） |
| 4 | **未承認のサブ画像が img2img の参照元になる**。承認列を一切見とらん | 中 | `functions/api/lib/route-context.ts:8501-8525` `getCharacterReferenceImageBase64` は `archivedAt` の IS NULL だけで絞って `ord` 降順1件。`approval` 列は実DBに存在する（`src/schema/character-visual.ts:70` に「実DBには approval / image_model / image_provider 列も存在する（schema未反映の歴史的ドリフト）」と明記）が、クエリからも Drizzle schema からも参照されとらん | **判断待ちから降ろした。**避けとる理由が要らん。同じ関数の `archivedAt` に付いとるコメント（「設定違反の旧画像を手本にすると新規生成がその見た目を引き継ぐ」）が、未承認画像へそのまま当てはまる。承認済みでない行を where から外す。avatar優先へ倒す案は敵対レビューで却下済みなんで、そっちへは触らん |
| 5 | **中出しが既定になっとらんかの判定が未測定**。反証は「条件化済みの節だけで説明でき反例0件」やったが、判定に要る台本が無いまま | 中 | `doc/backlog-2026-08-17.md:235,306`。`script/verify/` 配下に該当台本なし | 「ユーザーが明示要求せんまま climax へ到達する台本」を1本足して通読する |
| 6 | **品質の撮り直し回数がダンプから読めん**。ヘッダの `retryCount` が実は `refusalRetryCount` の複製やったんで、名前ごと落とした。正しい値を出す実装はまだ無い | 中 | commit d4cc49d の本文が自分で「The honest state is that the quality retake count is not readable from the dump at all — it lives in the worker log and quality_measurement. Surfacing it properly is separate work」と書いとる | worker ログか `quality_measurement` から品質撮り直し回数を拾ってダンプヘッダへ出す。これが無いと床の効き目を読解から判定でけへん（phase66 §4 で一度読み違えとる） |
| 7 | **コピーが握り潰される**。グループチャット画面だけ | 小 | `src/component/chat/message-bubble.tsx:780-782` が `void navigator.clipboard.writeText(content); toast.success("コピーしました")` のまま。reject を catch しとらん。正しい実装（try/catch＋execCommand フォールバック）は `src/lib/clipboard.ts` の `copyText` に既にあって `chatscope-input-bar.tsx` では使われとる。1対1の主画面は 08-19 の YAGNI 整理でコピー自体を撤去済みなんで、日常経路やない | `message-bubble.tsx:781` の `onCopy` を `src/lib/clipboard.ts` の `copyText` へ差し替える |
| 8 | **最初に話すのはどっちか**。ナンパならこちらから、の第一声側が未着手 | 小 | `doc/backlog-2026-08-17.md:239,323`。主導権をプロンプトへ足す案は `prompt/instructions/no-injected-ai-filter.md` 抵触で却下のまま | プロンプト注入やのうて構造側で。オンボーディングでシナリオの開始位置と最初の発話者を選ばせる |
| 9 | **migration の掃除**。消えたコミック機能の3本と 0017 の番号重複 | 小 | `drizzle/0006_comic.sql` `0008_moments.sql` `0046_comic_series.sql` が現存。`0017_cleanup_short_conversations.sql` と `0017_eminent_iceman.sql` の番号重複も現存 | 本番D1の適用済み記録と突き合わせてからリネーム／削除。実害ゼロなんで優先度は低いまま据え置きでええ |

---

## 3. 局長の判断待ち

2件だけ。ここに置いてええんは「局長本人が試すと明言した設計の好み」と「契約上の停止条件」の2つで、他は全部降ろした。

| 何を | なんでAIで決められんか | 何が要るか |
|---|---|---|
| **ト書きとキャラシートが矛盾した時の優先順位**（#1458 / 旧C4・F1） | 設計の好みの選択で、局長が「自分で試したい」と明言しとる。試した感触が決めるんで、こっちで先回りすると外す | 局長が自分で試した結果。AI側の追加調査は要らん |
| **予算の停止規則**（#15 / 旧「進める順-0」） | 契約が「165上限・145で伺う」やのに実績275ターンで、そのまま phase66 まで走り続けとる。停止・再承認の記録が `doc/` 配下に無い。予算は後戻りでけへん外向きの支出やんで、継続可否は局長のもん | 実績275ターン対計画165/145を出して、継続するか止めるかの一言 |

**降ろしたもの**: F2/G2（未承認サブ画像）は §2-4 へ移した。承認列を見とらんのを直すのに局長の好みは要らん——同じ関数の既存コメントが理屈をもう書いとる。

---

## 4. 片付いたもの

蒸し返さんでええやつ。証拠つき。

### 直した（53件）

| 項目 | 証拠 |
|---|---|
| A2 memory_note が作られん | commit 890815a／現行 `src/lib/api.ts:1267` に `/api/memory/extract` 呼び出し |
| A3 古い有効ノートが土俵に上がらん | commit 9fbf6bb + 61469e7／`route-context.ts:6329` `fetchRecentMemoryNotes`、呼び出し `:6701`（ベクタ検索は未実装のまま据え置き） |
| A4 場所・服装・進行度が嘘を吐く | commit 243eedb, ea60e11, 33d0df7／`route-context.ts:7134` `buildSceneStateContinuity`、`:7081` に「うち」語彙 |
| B1 霜月鈴の反転アーク | phase66 で 6/6 再確認（`vlong-2026-08-20-phase66.md:286-305`） |
| B2 外枠の書き分け | `drizzle/0069_`, `0070_`, `0071_` 現存。phase66 で再発報告なし |
| B3 L2の全文・表・別プロセス突き合わせ | phase66 L2-c 一致率 46/48=95.8%（phase54は86.5%）、ターン判定3/3一致 |
| B4 敵対レビュー運用 | `prompt/instructions/adversarial-review-with-codex.md` が恒久ルール。実例として 4275669 が 1041db9 で撤回されとる |
| C1 感覚指示が2字断片で誤爆 | commit 6cc1100／`sensory-mandate-rotation.test.ts` |
| C3 促しだけで1ターン進む | commit 4e5b526／`prompt-only-turn-holds-pace.test.ts` |
| C5 関係段階に合わん語彙 | 往復数を事実として渡す修正。phase54/66 で表1#2 は主要因子に出とらん |
| C9 / E5 語尾「〜なの」の乱発 | `drizzle/0068_sakura_ending_overuse.sql`（`default-character`, `char-koharu-ex`） |
| C12 交互指示が medium へ届いとらん | `[[route]].ts:1474` の非very_long分岐に交互指示あり。erotic/climax 双方（`route-context.ts:1199, 1216`）にも存在 |
| C13 medium で90〜240字しか返らん | `[[route]].ts:1472-1474`、`insertSystemDirectiveBeforeLastUser` は長さ種別を問わず実行 |
| C14 霜月鈴が erotic へ上がらん | `drizzle/0069_`。phase54/66 で Downer が実際に erotic/climax へ到達 |
| C15 `<inner>` 畳み込みが very_long 限定 | `route-context.ts:3949-3957`、コメントに「畳み込みは全長さで掛ける」 |
| C16 見本を削ったらXMLが崩れた退行 | `[[route]].ts:1479` の2サイクル雛形（a266563）＋ body-wall ヒント配線（8ebf9ba） |
| C17 / 08-19 地の文の壁 | commit 8ebf9ba／`route-context.ts:4181-4189` の `reasonHints` 配線。phase66 で壁の × は0件 |
| D1 つづきから無反応 | `ou-profile-screen.tsx:19,148,389`, `ou-home-screen.tsx:66`, `ou-scene-picker.tsx:260` |
| D2 タップで再送が無反応 | `you-message.test.tsx:55-59`（pointerup→pointerleave→click 順のテスト） |
| D4 リンクボタン | `her-message.tsx:849` に受け口。ボタン自体は 08-19 の YAGNI 整理で主画面から撤去 |
| D5 good/bad が逆三角 | `message-action-menu.tsx:3,92,104` ThumbsUp/ThumbsDown |
| D6 / D13 三点リーダーとメニュー位置 | `her-message.tsx:191-199` `withLoweredEllipsis`（vertical-align化、コメントにD13明記） |
| D7 チップのYAGNI撤去 | `chips-panel.tsx:7` |
| D8 写真スイッチが2つ | `settings-store.ts` の `imageProvider` が単一enum |
| D9 3層表示 | `message-bubble.tsx:176-194` action/dialogue/inner が独立p要素 |
| D10 返信待ちに入力でけへん | `chatscope-input-bar.tsx:102`、textarea に disabled を渡さず `sendOnReturnDisabled` のみ |
| D11 pull-to-refresh で追い出される | commit 40cd4dc |
| D12 / 08-19 ちらつき | commit 444fa62／`her-message.tsx:414-436` `RewritingIndicator` |
| D14 長い返信でスクロールでけへん | `ou-app.tsx:1016-1049` `isScrollTouchingRef` |
| D15 勝手に上へスクロール | `ou-app.tsx:1060-1076` `scrolledFocusIdRef`（コメントにD15明記）＋ `ou-app.test.tsx:788,824,846` |
| D16 履歴を消す導線が無い | `use-chat-query.ts`, `ou-log-screen.test.tsx`。#1459 |
| D17 返信候補 | `reply-suggestion-panel.tsx`, `functions/api/lib/reply-suggestion.ts`, `ouse-composer.tsx:240-249` |
| 08-19 三人称の小説調 | `drizzle/0070_`（「キャラ名主語」許可を削除）＋ `quality-guard.ts:1061,1737` `checkNoThirdPersonNarration` |
| 08-19 打ち切りが「」を開いたまま | commit edf8a2f |
| 08-19 場面がカフェのまま | commit 243eedb（学校語彙 4/20→0/20 実測） |
| 08-19 読み手が居らんのに上流6本 | commit 87bbfa1／`route-context.ts:5403` で `clientSignal?.aborted` を明示チェック |
| 08-19 フロア到達分が選別で負ける | commit 87bbfa1（同一コミット） |
| #1493 リロードで表示が消える | commit d9897fd |
| #1457 タップ再送が効かん | commit d9897fd／`ou-app-retry-send-feedback.test.tsx:123` |
| #1488/#1489 ボタンが並びすぎ | commit d9897fd／`her-message.tsx:846-858`（再生成のみ表示、残りはメニュー内） |
| #1487 さくらのプロフィール | commit aa4f926 |
| #1486 ダウナーの「あんた、誰？」 | commit aa4f926（同一コミット） |
| #1483 バックグラウンドで送信失敗 | commit 4c3f64c／`ou-app-background-send.test.tsx:138,155,170,177` |
| 測定ハーネス: 挨拶ターン未計上 | `script/verify/vlong-session-request-shape.test.ts:105-124` |
| 測定ハーネス: latencyMs の罠 | 08-19 に発見、以降の読解基準へ反映。ダンプ側の名前の食い違いは d4cc49d で除去（残件は §2-6） |
| E1 表示名「コウスケ」 | `functions/api/__tests__/user-display-name.test.ts:1-40`。本番D1の実値はこの環境から見えんが実害ゼロ |
| E2 関係性をデート前へ | `drizzle/0066_sakura_first_meeting_not_after_the_date.sql` |
| E3 グリーティング差し替え | `drizzle/0067_sakura_greeting_and_dislikes.sql` |
| E4 「嫌う：暴力、無理やり」の削除 | `drizzle/0067_`（REPLACE） |
| E6 チャット履歴全削除 | 87会話/491メッセージ/10 scene state を削除、キャラ221体は無傷。一回限りの運用作業 |
| G1 画像に conversationId が渡っとらん | `src/lib/api.ts:663-685`, `route-context.ts:332`（#444）, `validateImageGenerationOwnership` |
| 読解アーム17本の欠落回収 | `script/verify/vlong-session-dogfood.ts` ＋ `doc/dogfood/` に phase20〜66 が蓄積 |

### 反証で覆った（5件）

| 項目 | どこで覆ったか |
|---|---|
| **「毎ターン全部送って64K窓を越え、先頭のシートが落ちる」**（A1/H1/H2/H3・進める順-3をまとめて1件） | サーバが上流へ投げる前に切っとる。`route-context.ts:718-745` `trimChatMessagesToBudget`、`:7523` `CHAT_TRANSPORT_MAX_CHARS=180_000`、`:7527` `CHAT_HISTORY_MAX_TURNS=25`、適用は `[[route]].ts:1089` と `:1499`。回帰テスト `chat-history-turn-cap.test.ts` あり。実測した69,000字/95通は上流に一度も届いとらん。加えて `grep -rn transforms functions/ src/` = 0件（圧縮設定なし）、`isTransientOpenRouterStatus`（`:2597-2598`）は今も429/502/503/504のみ＝窓超過エラーが吹き出しに出る経路も無い。全部こっちで再実行して確認済み |
| C2 髪飾りグリーティング直後の矛盾台詞 | 修正後コーパス（phase20-25）で再現0件。グリーティング差し替え（`drizzle/0067`）で当該文言自体が消えとる |
| C10 生々しさが足りん | 機械では測れん。単独claimとして成立させる証拠が最初から無く、L2-bの体験表・芯の軸へ吸収。独立項目としては終了 |
| C11 清楚な子の抵抗が無い | 全ターン抵抗ゼロやのうて6ターン中2ターン。さくらのシートの急速献身アークと整合。指示追加は `no-injected-ai-filter.md` 抵触で禁止 |
| 進める順-4 呼びかけ専用リトライ枠 | 食われとるのは538ターン中6件(1.1%)。カテゴリ別上限は `if (maxRetries < X)` の底上げにしか使われず本番初期値3では発火せん。実際の直しは commit fe1e7df の別経路 |

---

## 5. 不明

| 何が | 何があれば決まるか |
|---|---|
| **旧台帳が引いとるコミットハッシュ2件が git 履歴に存在せん**（`b569d1a2`=C15, `a664c41a`=C12/C13）。`git cat-file -e` が両方 `fatal: Not a valid object name` を返す。squash/rebase で消えたSHAを引いとると見とる | 機能自体は問題なし——`<inner>` 畳み込みの全長さ適用も `<action>`/`<dialogue>` の交互配置も、コードを開いて現行状態を直接確認できとる（上の表を見い）。要るんは正しいSHAの特定だけで、`git log --all --oneline --format='%H %s'` を squash 前のリモート参照込みで当たれば付き合わせられる。**この台帳では該当行のハッシュを外して file:line の証拠へ差し替えた**んで、機能面の残件やない |

なお旧台帳の「C9 は再現せん」という分類は誤りやった（実際は `drizzle/0068` で直っとる）。この台帳の §4 で 直した へ訂正済みなんで、そっちの表記残件はこれで閉じる。
